# Pinned Lemonade embeddable release, vendored into the packages by CI.
# Must match PINNED_VERSION / PINNED_SHA256 in src/dictatr/backend/lemond.py
# (lemond.py cannot source this file); tests/test_backend.py asserts the
# two pins agree. sha256 is for lemonade-embeddable-<ver>-ubuntu-x64.tar.gz.
LEMOND_VERSION=11.8.0
LEMOND_SHA256=3cb13e93b0496c583e4cb4dda6aef58c39fc71fbb058fb171d62ac18f4cd72fc
