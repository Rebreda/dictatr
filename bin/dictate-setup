#!/bin/bash
# Setup wizard: an overlay like the menu and the chat where layer-shell
# exists, a plain centered window where it does not.
. "$(dirname "$(readlink -f "$0")")/_common.sh"
preload_layer_shell
exec "$py" "$repo/ui/setup.py" "$@"
